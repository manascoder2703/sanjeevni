// A slot stays blocked only while the appointment still needs to happen.
export const OCCUPYING_APPOINTMENT_STATUSES = ['pending', 'confirmed'];

export const RELEASING_APPOINTMENT_STATUSES = ['completed', 'cancelled', 'rejected'];
